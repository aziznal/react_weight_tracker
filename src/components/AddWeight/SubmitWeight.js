

const SubmitWeight = ({ onEntrySubmit }) => {
    return (
        <div className="row">
            
            <button className="btn" onClick={onEntrySubmit}>
                Submit Entry
            </button>

        </div>
    )
}

export default SubmitWeight
